package cz.zcu.kiv.pia.silhavyj.socialnetwork.model.token;

/***
 * Token type
 *
 * @author Jakub Silhavy (A21N0072P)
 */
public enum TokenType {

    /*** registration token (used when finishing up the process of registration) */
    REGISTRATION,

    /*** reset password token (used when resetting user's password) */
    RESET_PASSWORD
}
